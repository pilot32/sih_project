import 'package:flutter/material.dart';

class AppColors {

  // Educational Color Palette - Playful & Safe (Ages 6-11)
  static const Color background = Color(0xFFF0F4F8); // Soft Cloud Blue
  static const Color foreground = Color(0xFF2D3748); // Soft Charcoal

  static const Color card = Color(0xFFFFFFFF);
  static const Color cardForeground = Color(0xFF2D3748);

  // Primary - Friendly Blue (Trust & Calm)
  static const Color primary = Color(0xFF4299E1); 
  static const Color primaryForeground = Color(0xFFFFFFFF);
  static const Color primaryGlow = Color(0xFF63B3ED);

  // Secondary - Energetic Orange (Motivation)
  static const Color secondary = Color(0xFFED8936);
  static const Color secondaryForeground = Color(0xFFFFFFFF);
  static const Color secondaryGlow = Color(0xFFFBD38D);

  // Success - Bright Green (Encouragement)
  static const Color success = Color(0xFF48BB78);
  static const Color successForeground = Color(0xFFFFFFFF);
  static const Color successGlow = Color(0xFF9AE6B4);

  // Muted - Soft Greys
  static const Color muted = Color(0xFFE2E8F0);
  static const Color mutedForeground = Color(0xFF718096);

  // Accent - Soft Purple (Creativity)
  static const Color accent = Color(0xFF9F7AEA);
  static const Color accentForeground = Color(0xFFFFFFFF);
  
  static const Color border = Color(0xFFCBD5E0);
  static const Color input = Color(0xFFEDF2F7);

  static const Color destructive = Color(0xFFE53E3E);
  static const Color destructiveForeground = Color(0xFFFFFFFF);

  // Subject Colors - Vibrant & Distinct
  static const Color hindiColor = Color(0xFFFF9F1C); // Bright Orange
  static const Color englishColor = Color(0xFF4CC9F0); // Sky Blue
  static const Color mathColor = Color(0xFFF72585); // Pop Pink (Fun!)
  static const Color scienceColor = Color(0xFF7209B7); // Deep Purple
  static const Color socialColor = Color(0xFF4361EE); // Royal Blue
  static const Color commerceColor = Color(0xFF4895EF); // Blue
  static const Color computerColor = Color(0xFF560BAD); // Dark Purple

  // Gradients for Cards (Soft & Dreamy)
  static const List<Color> primaryGradient = [Color(0xFF4299E1), Color(0xFF63B3ED)];
  static const List<Color> secondaryGradient = [Color(0xFFED8936), Color(0xFFF6AD55)];
  static const List<Color> successGradient = [Color(0xFF48BB78), Color(0xFF68D391)];
  static const List<Color> learningGradient = [Color(0xFF4CC9F0), Color(0xFF4361EE)];
  static const List<Color> heroGradient = [Color(0xFFF72585), Color(0xFF7209B7)];

  // Dark Theme Colors (Restored)
  static const Color backgroundDark = Color(0xFF0F172A);
  static const Color foregroundDark = Color(0xFFF1F5F9);
  static const Color cardDark = Color(0xFF1E293B);
  static const Color mutedDark = Color(0xFF334155);
  static const Color mutedForegroundDark = Color(0xFF94A3B8);
  static const Color borderDark = Color(0xFF334155);
}